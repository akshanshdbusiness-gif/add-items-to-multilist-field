export default function Home() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'inherit' }}>
      <h1>Add Items to Multilist Field</h1>
      <p>
        This is a Sitecore Marketplace app. It has no standalone UI — it runs as a
        Page Builder Custom Field extension at <code>/add-item</code>, embedded in the
        SitecoreAI Page builder, not opened directly in a browser.
      </p>
    </main>
  )
}
